server:
  port: 8201  # Example port for manager-service

spring:
  application:
    name: manager-service
  datasource:
    url: jdbc:postgresql://localhost:5432/employeetaskmanager
    username: postgres
    password: 123

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
